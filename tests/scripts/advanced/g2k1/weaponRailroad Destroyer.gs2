//#CLIENTSIDE
function onBomyWeaponFired() {
  onWeaponFired();
}

function onWeaponFired() {
  triggeraction(player.x + 1.5 + vecx(player.dir) * 2, player.y + 2 + vecy(player.dir) * 2, "removerail", null);
}